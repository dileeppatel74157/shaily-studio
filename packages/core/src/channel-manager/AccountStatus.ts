// ─── Account Status ───────────────────────────────────────────────────────────

export enum AccountStatus {
  CONNECTED          = "CONNECTED",
  DISCONNECTED       = "DISCONNECTED",
  EXPIRED            = "EXPIRED",
  RECONNECT_REQUIRED = "RECONNECT_REQUIRED",
  SUSPENDED          = "SUSPENDED",
  ERROR              = "ERROR",
}
