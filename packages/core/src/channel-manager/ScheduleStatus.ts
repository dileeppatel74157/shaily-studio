// ─── Schedule Status ──────────────────────────────────────────────────────────

export enum ScheduleStatus {
  PENDING   = "PENDING",
  ACTIVE    = "ACTIVE",
  COMPLETED = "COMPLETED",
  MISSED    = "MISSED",
  FAILED    = "FAILED",
}
