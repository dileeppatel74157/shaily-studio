// ─── Budget State ─────────────────────────────────────────────────────────────

export enum BudgetState {
  NORMAL        = "NORMAL",
  WARNING       = "WARNING",
  LIMIT_REACHED = "LIMIT_REACHED",
  BLOCKED       = "BLOCKED",
  RECOVERING    = "RECOVERING",
}
