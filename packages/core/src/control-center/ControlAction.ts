// ─── Control Action ───────────────────────────────────────────────────────────

export enum ControlAction {
  START    = "START",
  STOP     = "STOP",
  PAUSE    = "PAUSE",
  RESUME   = "RESUME",
  RESTART  = "RESTART",
  SKIP     = "SKIP",
  RETRY    = "RETRY",
  ROLLBACK = "ROLLBACK",
}
