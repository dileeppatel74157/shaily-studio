// ─── Override Type ────────────────────────────────────────────────────────────

export enum OverrideType {
  NONE      = "NONE",
  MANUAL    = "MANUAL",
  FORCE     = "FORCE",
  PARTIAL   = "PARTIAL",
  TEMPORARY = "TEMPORARY",
  PERMANENT = "PERMANENT",
}
