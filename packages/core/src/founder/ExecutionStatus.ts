// ─── Execution Status ─────────────────────────────────────────────────────────

export enum ExecutionStatus {
  IDLE      = "IDLE",
  RUNNING   = "RUNNING",
  WAITING   = "WAITING",
  BLOCKED   = "BLOCKED",
  FAILED    = "FAILED",
  COMPLETED = "COMPLETED",
}
