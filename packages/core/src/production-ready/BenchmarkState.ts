// ─── Benchmark State ─────────────────────────────────────────────────────────

export enum BenchmarkState {
  IDLE      = "IDLE",
  RUNNING   = "RUNNING",
  COMPLETED = "COMPLETED",
  FAILED    = "FAILED",
}
