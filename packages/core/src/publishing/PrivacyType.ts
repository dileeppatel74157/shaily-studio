// ─── Privacy Type ─────────────────────────────────────────────────────────────

export enum PrivacyType {
  PUBLIC       = "PUBLIC",
  UNLISTED     = "UNLISTED",
  PRIVATE      = "PRIVATE",
  MEMBERS_ONLY = "MEMBERS_ONLY",
}
